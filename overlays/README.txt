Upload PNG / GIF / WebP / JPG / WebM objects from the app (right-click → Add object),
or drop files here. They show up as overlaydrop__… and work in preview + export.

Bundled samples in public/overlays/ are optional — hide them in the Object picker
if you only want your own uploads.
